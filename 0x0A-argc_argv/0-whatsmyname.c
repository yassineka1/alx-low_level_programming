#include <stdio>

/**
 * main - Prints the name of the program
 * @argc: Number of arguments
 * @agrv: Array of pointers to arguments
 * Return: Sucess
 */

int main(int __attribute__((__unused__)) argc,char *argv[])
{
	printf("%s\n", argv[0]);
	return (0);
}
